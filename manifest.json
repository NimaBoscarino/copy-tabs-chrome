{
  "name": "Copy Tabs",
  "version": "0.3",
  "description": "Creates a keyboard shortcut (Alt + Shift + P) to copy the selected tabs",
  "background": {
    "persistent": false,
    "scripts": ["background.js"]
  },
  "commands": {
    "copy-tabs": {
      "suggested_key": { "default": "Alt+Shift+P" },
      "description": "Copy the links to the selected tabs"
    }
  },
  "permissions": [
    "tabs"
  ],
  "manifest_version": 2
}
